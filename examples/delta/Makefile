include $(APPDIR)/Make.defs

PROGNAME  = $(CONFIG_EXAMPLES_DELTA_PROGNAME)
PRIORITY  = $(CONFIG_EXAMPLES_DELTA_PRIORITY)
STACKSIZE = $(CONFIG_EXAMPLES_DELTA_STACKSIZE)
MODULE    = $(CONFIG_EXAMPLES_DELTA)

CSRCS = c_scheduler.c
MAINSRC = delta_bench_main.c

include $(APPDIR)/Application.mk
